Update docs and changelog for user-facing changes.

Inputs:
- $ARGUMENTS: optional focus area or file paths.

Context (run first):
! git status
! git diff --stat
! git diff

Steps:
1) Identify user-facing behavior changes in the diff.
2) Update README/docs/changelog with minimal edits.
3) Summarize documentation changes.
