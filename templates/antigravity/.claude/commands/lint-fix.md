Run lint/format and fix failures.

Context (run first):
! git status
! git diff --stat

Steps:
1) Run the lint task or the lint command from `CLAUDE.md`.
2) Fix issues with minimal edits.
3) Re-run lint until clean.
