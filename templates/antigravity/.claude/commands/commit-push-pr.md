Commit, push, and open a PR.

Context (run first):
! git status
! git diff --stat

Steps:
1) Propose a concise commit message based on the diff.
2) Commit and push to the current branch.
3) Draft a PR title + description (What/Why/How/Testing).
4) Output the PR URL or the exact command to open it.
