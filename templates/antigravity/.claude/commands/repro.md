Reproduce a bug or report.

Inputs:
- $ARGUMENTS

Context (run first):
! git status

Steps:
1) Turn the report into deterministic reproduction steps.
2) Find the smallest failing test or add a minimal repro.
3) Capture logs, propose a fix, and verify.
